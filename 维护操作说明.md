# Manifold Tech Wiki 维护操作说明（本地自用）

## 1. 这份 Wiki 是什么结构

- **站点类型**：Jekyll 静态站点
- **主题**：`just-the-docs/just-the-docs`（通过 `remote_theme` 引用）
- **主要内容目录**：`wiki/docs/`
  - **站点配置**：`wiki/docs/_config.yml`
  - **站点首页（Overview）**：`wiki/docs/index.md`
  - **Odin 文档分组**：`wiki/docs/odin_series/`

> 说明：`just-the-docs` 的侧边栏导航通常由每个页面的 front matter（YAML 头）里的 `nav_order` / `parent` / `has_children` 等字段自动生成。

---

## 2. 最常见的维护：修改现有内容

### 2.1 修改某一页的正文

- 直接打开对应的 `*.md` 文件编辑即可。
- Markdown 里可以写标题、列表、代码块、表格等。

建议：
- 标题层级从 `#` 开始，尽量不要跳级。
- 代码块用三引号：
  
```md
```bash
command
```
```

### 2.2 修改某一页在左侧导航里的显示名称

每个页面顶部通常有一段 YAML 头（front matter），例如：

```yaml
---
title: Odin1
parent: Odin Series
nav_order: 1
has_children: true
---
```

- **改导航显示名**：改 `title`。
- **不想出现在导航里**：可以设置 `nav_exclude: true`（如果你们想用这个能力）。

---

## 3. 导航（侧边栏）怎么控制

### 3.1 控制排序：`nav_order`

- 同一层级下，数值越小越靠前。
- 示例：`wiki/docs/odin_series/index.md` 里 `nav_order: 1`，会排在比较靠前的位置。

### 3.2 父子层级：`parent` 与 `has_children`

- `has_children: true` 表示该页面下面会挂子页面。
- 子页面通过 `parent: <父页面 title>` 归类到父页面下。

示例（已存在）：
- 父页面：`wiki/docs/odin_series/index.md`
  - `title: Odin Series`
  - `has_children: true`
- 子页面：`wiki/docs/odin_series/odin1/index.md`
  - `parent: Odin Series`
  - `has_children: true`

### 3.3 常见坑：`parent` 必须和父页面 `title` 完全一致

- 包括大小写、空格、标点。
- 如果不一致，页面会“掉到别的地方”或变成顶层。

---

## 4. 新增一页（最常用）

以 Odin1 子章节为例（假设你要加第 11 章）：

1. 在目录 `wiki/docs/odin_series/odin1/` 下新建一个 Markdown 文件，例如：
   - `11. New Chapter.md`
2. 在文件顶部加入 front matter（决定它在导航中的位置）：

```yaml
---
title: 11. New Chapter
parent: Odin1
nav_order: 11
---
```

3. 写正文。

> 注意：这里的 `parent: Odin1` 要和 `wiki/docs/odin_series/odin1/index.md` 的 `title: Odin1` 对上。

---

## 5. 新增一个“分组首页”（类似 Odin Series）

如果你将来要做另一个产品分组（例如 Pocket），正确做法一般是：

1. 创建一个新目录，例如：`wiki/docs/<product>_series/`
2. 创建 `index.md`，设置：

```yaml
---
title: <Product> Series
nav_order: 2
has_children: true
---
```

3. 在该目录下继续加子目录/子页面，并用 `parent` 组织层级。

> 你当前的 Odin 就是按这个模式做的：`docs/odin_series/index.md` → `docs/odin_series/odin1/index.md` → 各章节页。

---

## 6. 链接写法与中文/空格文件名注意事项

你们现有文档里已经在用“文件名带空格/带点号”的链接形式，例如：

- `[阅读提示](Reading%20Tips)`
- `[1. 产品概述](1.%20Product%20Overview_)`

结论：
- **文件名包含空格时**，链接里要写成 `%20`。
- **建议做法**：新增文件尽量用英文 + 不带空格（用 `-`），能减少链接问题。

---

## 7. 图片/附件怎么放

在现有结构里，你们已经有类似：
- `wiki/docs/odin_series/odin1/assets/...`

建议约定：
- 图片/附件放在该产品目录下的 `assets/`，避免全站混在一起。
- 在 Markdown 里引用图片：

```md
![alt text](./assets/img/your-image.png)
```

---

## 8. 站点配置改哪里（谨慎）

主要配置在：`wiki/docs/_config.yml`

你们当前关键项：
- `remote_theme: just-the-docs/just-the-docs`
- `baseurl: "/wiki"`
- `defaults` 里对 `odin_series` 路径开启了 `nav_enabled/search_enabled`

一般内容维护不需要动 `_config.yml`。

---

## 9. Git 更新/提交流程（删掉本地后重新拉取也适用）

你后续如果打算把本地文件都删掉，等有更新时再重新拉一份到本地编辑，这个流程是完全 OK 的。核心就是：

1. `git clone` 拉取最新仓库
2. 本地修改 `docs/**/*.md`
3. `git add` / `git commit`
4. `git push`

下面按最常用场景给出“照着敲就能用”的步骤。

### 9.1 第一次拉取/本地删光后重新拉取

```bash
git clone https://github.com/ManifoldTechLtd/wiki.git
cd wiki
```

> 说明：仓库 URL 以你们实际为准；如果你是 SSH，则一般是 `git@github.com:ManifoldTechLtd/wiki.git`。

### 9.2 修改内容

- 通常只需要改：`docs/**/*.md`
- 改完建议先看一下变更：

```bash
git status
git diff
```

建议（push 前本地预览）：
- 如果你需要在 localhost 先确认页面效果，再去提交/推送，请先看下面的 **第 10 章《本地预览》**。

### 9.3 提交并推送（push）

```bash
git add -A
git commit -m "docs: update odin wiki"
git push
```

建议：
- 每次提交信息（`-m`）写清楚改了什么，比如：`docs: update Odin1 installation guide`。

### 9.4 你不是每次都重新 clone：如何同步远端最新再继续改

如果你本地还留着仓库目录（没有删），更新步骤一般是：

```bash
git pull --rebase
```

如果你不习惯 rebase，也可以用：

```bash
git pull
```

### 9.5 遇到冲突（conflict）怎么办（最小处理）

- 先看哪些文件冲突：`git status`
- 打开冲突文件，手动保留正确内容，删掉冲突标记：
  - `<<<<<<<`
  - `=======`
  - `>>>>>>>`
- 处理完后：

```bash
git add <冲突文件>
git rebase --continue
```

如果你用的是 `git pull`（merge）而不是 rebase，则冲突解决后：

```bash
git add <冲突文件>
git commit
```

### 9.6 不要提交的本地文件（建议）

如果你本地会生成一些临时文件，建议确保不要被提交，例如：
- Jekyll 构建目录：`_site/`
- 缓存目录：`.jekyll-cache/`

通常这类在 `.gitignore` 里处理；提交前用 `git status` 看一眼，可以避免误提交。

---

## 10. 本地预览（可选）

你当前这个目录（`/home/hugo/wiki/wiki`）里**没有** `Gemfile`，但在 `wiki_v2/` 里有一套可用的 Ruby 依赖：
- `/home/hugo/wiki/wiki_v2/Gemfile`
- `/home/hugo/wiki/wiki_v2/Gemfile.lock`

如果你需要本地预览（localhost 看效果），一般有两种思路（选一种即可）。

预览成功后再回到第 9 章继续 `git commit` / `git push`。

### 10.1 直接在 `wiki_v2` 下启动（推荐先按现状走）

在 `wiki_v2` 里已经声明了：
- `jekyll ~> 4.4`
- `just-the-docs`
- `jekyll-remote-theme`

你可以用 `wiki_v2` 作为“构建环境”，但让它去 serve 当前目录 `wiki/docs` 的内容：

```bash
# 在 /home/hugo/wiki/wiki_v2 目录下
bundle install
bundle exec jekyll serve \
  --source ../wiki/docs \
  --config ../wiki/docs/_config.yml
```

启动后一般访问：
- `http://127.0.0.1:4000/wiki/`

如果你希望本地访问时不带 `/wiki`，可以尝试（可选）：

```bash
bundle exec jekyll serve \
  --source ../wiki/docs \
  --config ../wiki/docs/_config.yml \
  --baseurl ""
```

然后访问：
- `http://127.0.0.1:4000/`

> 说明：是否需要 `--baseurl ""` 取决于你本地访问习惯；因为你们当前配置里 `baseurl: "/wiki"`。

### 10.2 把 `wiki_v2` 的 Gemfile 复制到当前目录（如果你想统一）

把 `wiki_v2/Gemfile` 和 `wiki_v2/Gemfile.lock` 复制到 `wiki/`，然后再按 Jekyll 常规方式启动。

复制完成后，启动命令通常是：

```bash
# 在 /home/hugo/wiki/wiki 目录下
bundle install
bundle exec jekyll serve --source ./docs --config ./docs/_config.yml
```

访问方式同上（看你是否保留 `baseurl: "/wiki"`）。

> 说明：我这里只写操作思路，不直接帮你改构建文件，避免影响你当前工作流。

---

## 11. 常见问题排查（内容维护角度）

- **页面不出现在侧边栏**
  - 检查是否缺少 front matter（`---` 开头那段）
  - 检查 `nav_exclude` 是否被开启
  - 检查 `parent` 是否写错（必须匹配父页面 `title`）

- **链接 404**
  - 文件名是否改过
  - 是否有空格没转义（`%20`）
  - 相对路径是否写对（`./`、`../`）

- **排序不对**
  - 检查 `nav_order` 是否重复或未填写

---

## 12. 建议的日常改动流程（最省心）

- 只改内容：直接改 `docs/**/*.md`
- 新增页面：新建 `md` + 写对 front matter
- 改文件名：同时全局搜索并更新引用链接（尤其注意 `%20`）

